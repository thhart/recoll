#!/bin/sh
# Debconf config script for recoll-semantic.
# Copyright 2026 ITTH GmbH & Co. KG
set -e

. /usr/share/debconf/confmodule

db_input high recoll-semantic/ollama-host || true
db_go || true

db_input high recoll-semantic/pull-model || true
db_go || true
